# ── Web 框架 ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
aiofiles==24.1.0

# ── 数据库 ──
sqlalchemy==2.0.36
alembic==1.14.0
pymysql==1.1.1

# ── 认证 ──
python-jose[cryptography]==3.3.0
bcrypt==5.0.0

# ── 配置 ──
python-dotenv==1.0.1
pydantic-settings==2.7.0

# ── 异步 HTTP ──
httpx==0.28.1

# ── 聊天问答缓存（Redis）──
redis==5.2.1

# ── Milvus 向量库（LlamaIndex 统一管理：dense 语义检索 + BGE-M3 学习稀疏 + RRF 混合检索）──
pymilvus>=2.5,<2.6

# ═══════════════════════════════════════
# AI/ML 依赖（镜像体积会膨胀到 8GB+，首次构建慢）
# ═══════════════════════════════════════

# ── LangChain 1.x（检索主链已迁移 LlamaIndex，仅保留记忆压缩与消息类型底座）──
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0

# ── LlamaIndex 0.12.x（独立生态，负责索引/检索/重排；与 LangChain 1.x 共存）──
# SentenceTransformerRerank 精排内置于 llama-index-core（core/postprocessor），无需单独 postprocessor 包
llama-index-core>=0.12,<0.13
llama-index-vector-stores-milvus>=0.6,<0.7
# FlagEmbedding：MilvusVectorStore 的默认稀疏编码器（BGE-M3 学习型稀疏，替代服务端 BM25；
# bge-m3 模型缓存于 model_cache volume，重建不重下）
FlagEmbedding==1.4.1

# ── 向量 & 模型 ──
# FastEmbed（ONNX 运行时，无 torch 依赖，避免 CUDA 大包下载）
fastembed>=0.4,<1.0
# transformers 用于 chunker 的 AutoTokenizer 精确 token 计数
transformers>=4.41,<5
# sentence-transformers 用于 rerank（CrossEncoder 加载 bge-reranker，依赖已装好的 torch）
sentence-transformers>=3.3,<4

# ── 文档抽取 ──
# MinerU 3.x（pipeline backend 需 torch>=2.6，由 Dockerfile 单独装 CPU 版）
mineru[pipeline]==3.4.4
pymupdf==1.25.1                 # MinerU 降级方案：PDF
python-docx>=1.2.0,<2           # MinerU 降级方案：DOCX（mineru 要求 >=1.2.0）
